project:
  type: default

# Nota: esta semana no se carga webR (no hay código R en la teoría).
# Si una semana futura lo necesita, restaurar el filtro y los paquetes:
#   filters: [webr]  |  webr: {packages: [dplyr, ggplot2]}

format:
  html:
    theme:
      light: lux
    number-sections: true
    toc: true
    toc-location: left
    toc-expand: 0
    smooth-scroll: true
    link-external-newwindow: true
    code-tools: false
    css: styles.css
